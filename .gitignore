# dependencies
node_modules

# next.js
.next
out

# env files
.env
.env*.local

# prisma
/src/generated/prisma
prisma/*.db
prisma/*.db-journal

# sqlite
*.db
*.db-journal
# DB backups (timestamped .bak-* suffix slips past the *.db glob — SEC-07 hygiene)
*.db.bak*
*.bak

# claude
.claude/
.playwright-mcp/

# Claude framework — never commit
CLAUDE.md
manifest.md
profile.md
content/
docs/testing-setup.md
docs/session-log.md
docs/session-handoff.md
docs/framework-issues.md

# misc
*.tsbuildinfo
next-env.d.ts

# audit-script data artifact — `audit-benefit-values.ts --apply corrections.json`
# per-run benefit-value correction map; data, never committed (Security Low)
corrections.json

# dev screenshots at project root (Playwright MCP default save location)
/*.png

# Design reference bundle (binary; referenced via design URL + design-overview-artifact memory, not committed)
docs/claudeDesignUI.zip
docs/claudeDesignUI_extracted/

# Feature 9 design source — decompressed JSX from the standalone HTML export (SEC-07 hygiene; reproducible, not committed)
docs/design-source/
